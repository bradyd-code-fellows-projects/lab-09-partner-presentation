'use strict';

describe('Auth router tests', () => {
  test.todo('POST /signup creates a new user and sends an object with the user and the token to the client');
  test.todo('POST /signin with basic authentication headers logs in a user and sends an object with the user and the token to the client');
});